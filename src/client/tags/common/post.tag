misskey-post(id="{opts.post.id}")
    span {opts.post.text}
    style.
        misskey-post {
            display:block;
            background: #fffbfb;
            padding: 28px 32px;
            margin: 0;
            margin-bottom: 1px;
        }
